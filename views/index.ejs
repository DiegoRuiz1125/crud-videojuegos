<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>🎮 CRUD de Videojuegos</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #ff6b6b;
    }
    .header {
      background-color: #343a40;
      color: white;
      padding: 20px;
      border-radius: 8px;
      margin-bottom: 30px;
    }
    .form-control {
      margin-bottom: 10px;
    }
    .card {
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="header text-center">
      <h1>🎮 Gestión de Videojuegos</h1>
      <p class="lead">Agrega, edita y elimina tus videojuegos favoritos</p>
    </div>

    <div class="card p-4 mb-4">
      <h4 class="mb-3">Agregar nuevo videojuego</h4>
      <form action="/agregar" method="POST" class="row">
        <div class="col-md-3"><input type="text" name="titulo" class="form-control" placeholder="Título" required></div>
        <div class="col-md-3"><input type="text" name="plataforma" class="form-control" placeholder="Plataforma"></div>
        <div class="col-md-3"><input type="text" name="genero" class="form-control" placeholder="Género"></div>
        <div class="col-md-3"><input type="date" name="lanzamiento" class="form-control"></div>
        <div class="col-12 mt-3"><button type="submit" class="btn btn-success w-100">Agregar Videojuego</button></div>
      </form>
    </div>

    <div class="card p-4">
      <h4 class="mb-3">Lista de Videojuegos</h4>
      <table class="table table-hover table-striped">
        <thead class="table-dark">
          <tr>
            <th>Título</th><th>Plataforma</th><th>Género</th><th>Lanzamiento</th><th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% videojuegos.forEach(v => { %>
            <tr>
              <td><%= v.titulo %></td>
              <td><%= v.plataforma %></td>
              <td><%= v.genero %></td>
              <td><%= v.lanzamiento.toISOString().slice(0, 10) %></td>
              <td>
                <a href="/editar/<%= v.id %>" class="btn btn-sm btn-warning">✏️</a>
                <a href="/eliminar/<%= v.id %>" class="btn btn-sm btn-danger">🗑️</a>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
