<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>✏️ Editar Videojuego</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #ff6b6b;
    }
    .container {
      max-width: 600px;
    }
    .card {
      margin-top: 50px;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      background-color: #ffffff;
    }
    .btn-success {
      width: 100%;
    }
    .form-label {
      font-weight: 500;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card">
      <h3 class="mb-4 text-center">✏️ Editar Videojuego</h3>
      <form action="/actualizar/<%= videojuego.id %>" method="POST">
        <div class="mb-3">
          <label class="form-label">Título</label>
          <input type="text" name="titulo" class="form-control" value="<%= videojuego.titulo %>" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Plataforma</label>
          <input type="text" name="plataforma" class="form-control" value="<%= videojuego.plataforma %>">
        </div>
        <div class="mb-3">
          <label class="form-label">Género</label>
          <input type="text" name="genero" class="form-control" value="<%= videojuego.genero %>">
        </div>
        <div class="mb-3">
          <label class="form-label">Fecha de lanzamiento</label>
          <input type="date" name="lanzamiento" class="form-control" value="<%= videojuego.lanzamiento.toISOString().slice(0,10) %>">
        </div>
        <button type="submit" class="btn btn-success mt-3">Guardar Cambios</button>
        <a href="/" class="btn btn-secondary mt-2 w-100">Cancelar</a>
      </form>
    </div>
  </div>
</body>
</html>
